version: 0.1             
component: build
timeoutInSeconds: 1000
shell: bash           

steps:
  - type: Command
    name: "Build app"
    command: |
      ./build_push_update_images.sh --prefix ocism 
      
outputArtifacts:
  - name: book-info-docker-images
    type: DOCKER_IMAGE
    location: build-service-demo
